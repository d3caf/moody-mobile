import React, {FunctionComponent} from 'react';
import {Layout, Text} from '@ui-kitten/components';

export const Entries: FunctionComponent<any> = props => {
    return (
        <Layout style={{flex: 1}}>
            <Text>Test</Text>
        </Layout>
    )
};
